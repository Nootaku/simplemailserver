{
  "name": "simplemailserver",
  "version": "1.0.0",
  "private": true,
  "description": "A simple mailserver to send contact info to your email.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "nodemailer": "^6.6.3"
  }
}
